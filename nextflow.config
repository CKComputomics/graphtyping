executor {
    name = 'k8s'
}

profiles {
	debug { process.beforeScript = 'echo $HOSTNAME' }
	docker {
		docker.enabled       = true
		docker.userEmulation = true
	}
}

k8s {
	runAsUser
	computeResourceType = 'Job'
	namespace = "analyst-services"
	storageClaimName = "graphtyper"
}
